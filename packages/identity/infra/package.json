{
  "name": "@relcko/identity-infra",
  "version": "0.1.0",
  "private": true,
  "description": "Identity v2.0 — infrastructure layer (event-store adapters, repository implementations, persistence)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@relcko/event-store": "workspace:*",
    "@relcko/identity-domain": "workspace:*",
    "@relcko/kernel": "workspace:*",
    "@relcko/scheduler": "workspace:*",
    "@relcko/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
